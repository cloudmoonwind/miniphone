node_modules/
dist/
*.log

# 环境变量
.env

# SQLite 数据库（运行时生成，含用户数据）
server/data/*.db
server/data/*.db-wal
server/data/*.db-shm

# 数据库备份目录与健康报告（含真实角色/对话数据）
server/data/backups/
server/data/health-latest.md

# AI 调用日志（含 prompt 全文）
server/data/logs/

# 迁移备份
server/data/*.json.bak

# API key 配置（换电脑需手动复制）
server/data/presets.json

# OS
.DS_Store
Thumbs.db
